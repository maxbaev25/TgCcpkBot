name: Restart TgCcpkBot on Remote Machine
on:
  pull_request:
    types:
      - closed
    branches:
      - master
  workflow_dispatch:
      inputs:
        logLevel:
          description: "Log Level"
          required: true
          default: "debug"

jobs:
  build:
    if: github.event.pull_request.merged == true
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Execute remote SSH commands using password
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: cd TgCcpkBot && git pull && sudo systemctl restart tgbot.service
